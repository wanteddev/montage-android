package com.wanted.android.wanted.design.menu

object WantedMenuContract {
    enum class ListType {
        Normal,
        Radio,
        Check
    }

    enum class DialogType {
        BottomSheet,
        Model
    }
}